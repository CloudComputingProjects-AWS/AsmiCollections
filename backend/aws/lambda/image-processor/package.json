{
  "name": "apparel-portal-image-processor",
  "version": "1.0.0",
  "description": "AWS Lambda image processor — generates WebP variants using Sharp",
  "main": "index.js",
  "dependencies": {
    "sharp": "^0.33.2",
    "@aws-sdk/client-s3": "^3.500.0"
  },
  "scripts": {
    "package": "npm install --production && zip -r image-processor.zip . -x '*.git*'"
  }
}
